#

## v1 - Incident Analysis Prompt

Used in `src/lib/prompt.ts` via `buildIncidentPrompt(history, userText)`.

Purpose:

- Build structured context for distributed systems incident analysis.
- Enforce output format for failure pattern, root cause, impact, and next checks.
